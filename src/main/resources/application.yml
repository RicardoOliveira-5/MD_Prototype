spring:
  application:
    name: transport-graph

  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: password

server:
  port: 8080

logging:
  level:
    org.springframework.data.neo4j: DEBUG
    pt.unl.fct: DEBUG
